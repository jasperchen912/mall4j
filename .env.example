# Template for the root .env file used by local Spring runs and Docker Compose.
# Keep values unquoted so Spring can import this file as properties.

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=yami_shops
MYSQL_USERNAME=root
MYSQL_PASSWORD=root
